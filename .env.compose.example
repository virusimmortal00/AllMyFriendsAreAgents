# Compose interpolation inputs. Merge into a private .env; do not overwrite an
# existing .env. Application settings and secrets belong in .env.container.
# Use a standalone checkout with its own .git directory, not a linked worktree.
AMFAA_PROJECT_PATH=.
AMFAA_HOST_PORT=53147
AMFAA_WEB_HOST_PORT=4173
# AMFAA_IMAGE=ghcr.io/virusimmortal00/all-my-friends-are-agents:main
